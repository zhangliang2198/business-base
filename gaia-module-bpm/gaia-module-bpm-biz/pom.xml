<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <groupId>com.zhen777.www</groupId>
        <artifactId>gaia-module-bpm</artifactId>
        <version>${revision}</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>
    <artifactId>gaia-module-bpm-biz</artifactId>

    <name>${project.artifactId}</name>
    <description>
        bpm 包下，业务流程管理（Business Process Management），我们放工作流的功能，基于 Flowable 6 版本实现。
        例如说：流程定义、表单配置、审核中心（我的申请、我的待办、我的已办）等等
    </description>
    <dependencies>
        <dependency>
            <groupId>com.zhen777.www</groupId>
            <artifactId>gaia-module-bpm-api</artifactId>
            <version>${revision}</version>
        </dependency>
        <dependency>
            <groupId>com.zhen777.www</groupId>
            <artifactId>gaia-module-system-api</artifactId>
            <version>${revision}</version>
        </dependency>

        <!-- 业务组件 -->
        <dependency>
            <groupId>com.zhen777.www</groupId>
            <artifactId>gaia-spring-boot-starter-biz-operatelog</artifactId>
        </dependency>
        <dependency>
            <groupId>com.zhen777.www</groupId>
            <artifactId>gaia-spring-boot-starter-biz-data-permission</artifactId>
        </dependency>
        <dependency>
            <groupId>com.zhen777.www</groupId>
            <artifactId>gaia-spring-boot-starter-biz-tenant</artifactId>
        </dependency>

        <!-- Web 相关 -->
        <dependency>
            <groupId>com.zhen777.www</groupId>
            <artifactId>gaia-spring-boot-starter-web</artifactId>
        </dependency>

        <dependency>
            <groupId>com.zhen777.www</groupId>
            <artifactId>gaia-spring-boot-starter-security</artifactId>
        </dependency>

        <!-- DB 相关 -->
        <dependency>
            <groupId>com.zhen777.www</groupId>
            <artifactId>gaia-spring-boot-starter-mybatis</artifactId>
        </dependency>

        <!-- Test 测试相关 -->
        <dependency>
            <groupId>com.zhen777.www</groupId>
            <artifactId>gaia-spring-boot-starter-test</artifactId>
        </dependency>
        <!-- 工作流相关 -->
        <dependency>
            <groupId>com.zhen777.www</groupId>
            <artifactId>gaia-spring-boot-starter-flowable</artifactId>
        </dependency>
    </dependencies>
</project>
